public class CafeJava{
    public static void main(String[] args){
        //APP VARIABLES
        // Lines of text that will appear in the app.
        String generalGreeting = "Welcome to Cafe Java, ";
        String pendingMessage = ", your order will be ready shortly. ";
        String readyMessage = ", your order is ready. ";
        String displayMessage = "Your total is $";
        String notReadyMessage = "Your order is not ready yet.";

        //Menu variables (add yours below)
        double coffeePrice = 5.50;
        double dripCoffee = 3.50;
        double latte = 4.50;
        double cappuccino = 4.50;

        //Customer name variable (add yours below)
        String customerNameOne = "Erick";
        String customerNameTwo = "Sam";
        String customerNameThree = "Jimmy";
        String customerNameFour = "Noah";

        //order completetions(add yours below)
        boolean orderReady1 = true;
        boolean orderReady2 = false;

        //APP INTERACTION SIMULATION

        // System.out.println(generalGreeting + customerNameOne + pendingMessage + readyMessage + displayMessage + coffeePrice);


        //If order is not ready
        // if(orderReady1){
        //     System.out.println(generalGreeting + customerNameFour + pendingMessage + readyMessage + displayMessage + cappuccino);
        // }
        // else if(orderReady2){
        //     System.out.println(notReadyMessage);
        // }

        //If order is ready
        // if(orderReady2){
        //     System.out.println(generalGreeting + customerNameFour + pendingMessage + readyMessage + displayMessage + cappuccino);
        // }
        // else if(orderReady1){
        //     System.out.println(notReadyMessage);
        // }


        // System.out.println(generalGreeting + customerNameTwo + pendingMessage + displayMessage + (latte + latte));
        // if(orderReady2){
        //     System.out.println(notReadyMessage);
        // }
        // else{
        //     System.out.println("Your order is ready");
        // }


        // System.out.println(generalGreeting + customerNameThree + pendingMessage + displayMessage + coffeePrice);
        // if(latte > coffeePrice){
        // System.out.println("Sorry about the mistake there Jimmy. " + displayMessage + (latte - coffeePrice));
        // }
        // else{
        // System.out.println("Sorry about the mistake there Jimmy. " + displayMessage + (latte) + " And heres your change back.");            
        // }
    }
}